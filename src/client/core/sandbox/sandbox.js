'use strict';

define(function (require) {
    var msg = require('core/sandbox/msg/msg');
    var util = require('core/sandbox/util/util');

    return {
        msg: msg,
        util: util
    };
    
});